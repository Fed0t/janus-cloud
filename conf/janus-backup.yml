
janus:
  queue_name: 'backup_upload'

  redis_connection: 'rediss://:hwgl39zgo4uupg5x@streams-redis-dev-do-user-1975712-0.b.db.ondigitalocean.com:25061/3'

  recordings_dir: "/opt/recordings/"

  max_recording_seconds: '120'

  mux_api_token: ""

  mux_api_secret: ""


admin_api:
  json: "indented"                      # Whether the JSON messages should be indented (default),
  # plain (no indentation) or compact (no indentation and no spaces)
  http_listen: '0.0.0.0:8300'           # REST API server listen addr
